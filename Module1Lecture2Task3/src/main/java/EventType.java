// EventType enum definition
public enum EventType {
    ARRIVAL,    // Customer arrives at the system
    DEPARTURE,  // Customer leaves the system
    SERVICE_START, // Customer starts being served
    SERVICE_END    // Customer finishes service
}